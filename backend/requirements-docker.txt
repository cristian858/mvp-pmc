# SafeSign AI - Dependencias Optimizadas para Apple Silicon y Docker
# Este archivo está optimizado para compilación dentro de Docker

# Core Flask
Flask==2.3.3
Flask-Login==0.6.3
Flask-SQLAlchemy==3.0.5
Flask-CORS==4.0.0
SQLAlchemy==2.0.20

# WSGI & Werkzeug
Werkzeug==2.3.7
greenlet==3.0.3

# Procesamiento de datos
numpy==1.24.3
pandas==2.0.3

# Procesamiento de documentos
PyPDF2==3.0.1
Pillow==10.0.0
python-dotenv==1.0.0

# OCR (Tesseract - binario en el sistema, pytesseract es solo wrapper)
pytesseract==0.3.13

# Visión por computadora (OpenCV)
# Nota: opencv-python se compila en el contenedor Docker
# En Apple Silicon, se usa la versión arm64 pre-compilada
opencv-python==4.8.0.76

# Testing
pytest==7.4.0
pytest-flask==1.2.0

# Nota sobre DeepFace/TensorFlow:
# Estas son OPCIONALES y se comentan porque requieren mucho espacio
# Descomenta solo si las necesitas. Se compilarán automáticamente en Docker.
#
# Para biometría avanzada (OPCIONAL):
# deepface==0.0.93
# tensorflow==2.13.0  # Requiere ~1GB en el contenedor
# protobuf==3.20.3
#
# Para ejecutar TensorFlow en Apple Silicon, se necesita:
# pip install tensorflow-macos==2.13.0  # (solo en Mac local, no en Docker)
